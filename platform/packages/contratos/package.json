{
  "name": "@trino/contratos",
  "version": "0.1.0",
  "private": true,
  "description": "Contratos compartilhados entre a API e o frontend: enums, códigos de erro, tipos de resposta e o cálculo puro de SLA",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
